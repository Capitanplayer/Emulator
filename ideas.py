# left side dictionary - functions + descriptions
# middle - code/instruction set
# right - registers, extra data
'''

3 columuns

first column text = dictionary
 parsed text of "key" + ": " + "info"

parsed text -> stored in array where every index is its own line

temp
if index > limit
string[index] = \n
else
temp = index
lamba da svas qasas
        ^
'''